# Core agent framework
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# Amazon Bedrock AgentCore runtime (Memory, Observability, Identity, Gateway).
# NOTE: the PyPI package for the "bedrock-agentcore-sdk-python" GitHub repo
# is published as `bedrock-agentcore` (import path: bedrock_agentcore).
bedrock-agentcore>=0.1.0
bedrock-agentcore-starter-toolkit>=0.1.0  # provides the `agentcore` CLI

# AWS SDK (Bedrock model invocation, AgentCore service calls)
boto3>=1.34.0

# Testing
pytest>=8.0.0

# Observability (OpenTelemetry -> AgentCore Observability / CloudWatch GenAI Observability)
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation>=0.46b0

# Misc utilities
python-dotenv>=1.0.1
